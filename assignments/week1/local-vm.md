# Local VM

## Prerequisites

The requirements for this setup are a Mac computer, Terminal, Multipass, and an internet connection. Multipass is used to run an Ubuntu virtual machine locally on the Mac.

## Installing Multipass

I installed Multipass on my Mac using the official installer from Canonical. After installing it, I checked that Multipass was working correctly by running:

```bash
multipass version
